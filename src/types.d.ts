// Lightweight module declarations to satisfy TypeScript when dev deps aren't installed
declare module 'mqtt';
declare module 'snarkjs';

// You can replace these with proper @types packages by running:
// npm install --save-dev @types/mqtt
